@{
    Layout = "_Layout";
}

@model IEnumerable<app.Models.ProductViewModel>

@if(Model.Any()) 
{
    @foreach(var product in Model) 
    {
    <a asp-controller="Products" asp-action="Details" asp-route-id="@product.ProductId">@product.ProductName</a>
    <br />
    }
}
